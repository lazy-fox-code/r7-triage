// Дата релизной сборки в миллисекундах. В исходнике — 0: это значит
// «отладка/тесты», и срок демоверсии тогда считается от установки (см.
// src/trial.js). При упаковке релиза scripts/build.mjs подставляет сюда
// дату релиза, и 90 дней отсчитываются от неё — так переустановка профиля
// больше не сбрасывает срок.
//
// Файл генерируется сборкой. Значение здесь править вручную незачем.
export const BUILD_DATE_MS = 0;
