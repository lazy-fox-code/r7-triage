node_modules/
dist/
*.xpi
.DS_Store

# Разметка для проверки качества — это реальная переписка, не для репозитория
eval/labeled.jsonl
eval/results/

# Служебные worktree Claude Code
.claude/

# Внутренние документы: в публичный репозиторий не идут, живут локально.
# Публичное описание — только README.md в корне.
/CLAUDE.md
/docs/

# Секреты и локальная конфигурация. В открытый код ничего секретного не
# попадает; ключ подписи, адреса и токены — здесь и только локально.
.env
.env.*
!.env.example
*.secret
